See, I told you not to look.

Open a Terminal

  $ sudo iptables -D OUTPUT -j SelfControl
  $ sudo iptables -F SelfControl
  $ sudo iptables -X SelfControl

The 'at' jobs will just run anyway and fail.
